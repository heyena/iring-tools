/*
 * File: Scripts/AM/view/directory/ScopeForm.js
 *
 * This file was generated by Sencha Architect version 2.2.2.
 * http://www.sencha.com/products/architect/
 *
 * This file requires use of the Ext JS 4.1.x library, under independent license.
 * License of Sencha Architect does not include license for Ext JS 4.1.x. For more
 * details see http://www.sencha.com/license or contact license@sencha.com.
 *
 * This file will be auto-generated each and everytime you save your project.
 *
 * Do NOT hand edit this file.
 */

Ext.define('AM.view.directory.ScopeForm', {
  extend: 'Ext.form.Panel',
  alias: 'widget.scopeform',

  node: '',
  bodyPadding: 10,
  bodyStyle: 'padding:10px 5px 0',
  url: 'directory/scope',

  initComponent: function() {
    var me = this;

    me.initialConfig = Ext.apply({
      url: 'directory/scope'
    }, me.initialConfig);

    Ext.applyIf(me, {
      defaults: {
        anchor: '100%',
        msgTarget: 'side'
      },
      dockedItems: [
        {
          xtype: 'toolbar',
          dock: 'bottom',
          items: [
            {
              xtype: 'tbfill'
            },
            {
              xtype: 'button',
              handler: function(button, event) {
                me.onSave();
              },
              text: 'Ok'
            },
            {
              xtype: 'button',
              handler: function(button, event) {
                me.onReset();
              },
              text: 'Cancel'
            }
          ]
        }
      ],
      items: [
        {
          xtype: 'hiddenfield',
          name: 'state'
        },
        {
          xtype: 'hiddenfield',
          name: 'scope'
        },
        {
          xtype: 'textfield',
          fieldLabel: 'Name',
          name: 'Name',
          allowBlank: false
        },
        {
          xtype: 'textareafield',
          fieldLabel: 'Description',
          name: 'Description'
        }
      ]
    });

    me.callParent(arguments);
  },

  onSave: function() {
    var me = this;
    var win = me.up('window');
    var form = me.getForm();
    var context = form.findField('Name').getValue();
    var state = form.findField('state').getValue();


    node.eachChild(function(n) {
      if(n.data.text == context) {
        if(state == 'new') {
          showDialog(400, 100, 'Warning', 'The name \"' + context + '\" already exits in this level, please choose a different name.', Ext.Msg.OK, null);
          return;
        }
      }
    });

    if(form.isValid()) {
      form.submit({
        waitMsg: 'Saving Data...',
        success: function (response, request) {
          win.fireEvent('save', me);
        },
        failure: function (response, request) {
          if (response.items[3].value !== undefined) {
            var rtext = response.items[3].value;
            showDialog(400, 100, 'Error saving folder changes', 'Changes of ' + rtext + ' are not saved.', Ext.Msg.OK, null);
            return;
          }
          var message = 'Error saving changes!';
          showDialog(400, 100, 'Warning', message, Ext.Msg.OK, null);
        }
      });
    } else {
      showDialog(400, 100, 'Warning', 'Please complete all required fields...', Ext.Msg.OK, null);
      return;
    }
  },

  onReset: function() {
    var me = this;
    var win = me.up('window');
    win.fireEvent('cancel', me);


  }

});