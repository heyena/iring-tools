<Window x:Class="iRTProxyConfig.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:iRTProxyConfig.ViewModel"
        xmlns:vw="clr-namespace:iRTProxyConfig.View"
        Title="Proxy Configuration" Height="350" Width="525" Icon="/iRTProxyConfig;component/Images/favicon.ico" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:ProxyParamsViewModel}">
            <vw:ProxyParamsView />
        </DataTemplate>
    </Window.Resources>
    <Grid Margin="4">
        <Border Background="GhostWhite" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5">
            <ItemsControl ItemsSource="{Binding ViewModels}" Margin="4" />
        </Border>
    </Grid>
</Window>
